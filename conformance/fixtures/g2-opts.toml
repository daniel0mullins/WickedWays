# g2-opts — authors the g2-opts golden pair. Proves the
# campaign `[opts]` table (previously hardcoded to the default): the real
# hollow-house bounds maxRounds 150 + baseEncounterChance 20. ASCII only.

title = "Opts"
startRoom = "Hall"

[opts]
maxRounds = 150
baseEncounterChance = 20

[[rooms]]
name = "Hall"
description = "A long central hall."
