# g2-timeout — authors the g2-timeout golden pair.
# Proves description.timeout_narration (previously hardcoded None): the real
# hollow-house onTimeout cue, authored as a plain `timeoutNarration` string that
# lowers to the { text } shape. The narration carries an em-dash, matched verbatim.

title = "Timeout"
startRoom = "Hall"

timeoutNarration = "Dawn never comes. You realize, slowly, that it never will — and that you stopped looking for the door some hours ago."

[[rooms]]
name = "Hall"
description = "A long central hall."
