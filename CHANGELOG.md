# Changelog

All notable changes to WickedWays are recorded here, newest release first. Entries are written
for players and campaign authors. Releases are cut with `cargo xtask release`, which aggregates
the pending changesets in [`.changesets/`](.changesets/README.md) into a section below and bumps
the single shared workspace version; versions follow [semantic versioning](https://semver.org).
